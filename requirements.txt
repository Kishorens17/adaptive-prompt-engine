# ─────────────────────────────────────────────────────────────────────────────
# Adaptive Prompt Engine — Dependencies
# ─────────────────────────────────────────────────────────────────────────────

# Core (always required)
python-dotenv>=1.0.0
sentence-transformers>=2.2.0    # ComplexityEstimator + SemanticCache embeddings
numpy>=1.24.0                   # Required by sentence-transformers + cache

# Testing
pytest>=7.4.0

# LLM providers (uncomment the one(s) you need)
google-genai>=1.0.0             # provider="gemini"
# openai>=1.0.0                 # provider="openai"

# REST API + dashboard
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0                 # bundled with fastapi, listed explicitly for clarity
